# NixOS

This repository contains my personal NixOS configuration.

## Installation

1. Boot from NixOS installer ISO
2. Run: `nix run github:nix-community/disko -- --mode disko ./disko.nix`
3. Install: `nixos-install --flake .#qurrie`

